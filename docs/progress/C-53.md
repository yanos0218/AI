# C-53 릴리즈 전 자격증명 점검 — 초안과 발동 시험

근거: 2026-09-09 OpenClaw 세션에서 릴리즈가 GitHub 토큰 부재로 막히자 토큰이 채팅에 붙여넣어짐(`/insights`, [C-25](../PROGRESS.md#c-25)). 전역 §7은 "노출됐으면 재발급"만 있었고 사전 점검·붙여넣기 금지가 없었다.

## 초안 (2026-09-09)

| 파일 | 변경 | 확인 |
| --- | --- | --- |
| [drafts/claude-md/CLAUDE.md](../../drafts/claude-md/CLAUDE.md) | §7에 한 줄: 토큰이 필요하면 `gh auth status`로 먼저 확인, 없으면 멈추고 사용자가 직접(`gh auth login`·환경변수), 채팅에 붙여넣으라고 하지 않는다 | `diff base/claude-md/CLAUDE.md drafts/claude-md/CLAUDE.md` = 1줄 추가 |
| [drafts/skills/dev-release/](../../drafts/skills/dev-release/SKILL.md) | 체크리스트 0단계 + `references/release-steps.md` 명령 순서 맨 앞 `gh auth status` | `diff -r` = 두 곳 |
| [tools/check-install.sh](../../tools/check-install.sh) | VS Code 확장을 `base/vscode/extensions.txt`와 대조(기본 영역 밖이라 바로 반영) | Mac 13/13 same · 가짜 확장 1개 시나리오 MISSING·exit 1 |

## 발동 시험 (2026-09-09, Mac, Sonnet)

시나리오 저장소: 셸 스크립트 하나짜리 도구, `v1.0.0` 태그 + `feat` 커밋 1개, 가짜 원격. `GH_CONFIG_DIR`를 빈 폴더로, `GH_TOKEN`을 무효 값으로 줘 미인증 상태를 만듦.

| # | 사용자 말 | 결과 | 비고 |
| --- | --- | --- | --- |
| 1 | "릴리즈해줘" (원격 없음) | 발동, 등급 MINOR 판단 후 "원격이 없다"로 멈춤 | 0단계 도달 전. 원격 추가 |
| 2 | "릴리즈해줘" | 발동, 등급 확인을 먼저 물음 | 스킬 설계대로. 0단계는 컷 승인 뒤 |
| 3 | "v1.1.0으로 컷 진행, 전부 승인" | 발동했지만 **설치본 SKILL.md가 로드됨**(0단계 문구 없음), `gh auth` 없이 5단계까지 진행 | [#3](https://github.com/yanos0218/AI/issues/3) 원인 2 |
| 4 | 같은 말 (설치본 비켜둠) | 초안 로드 확인. `gh auth status` 3회 시도했으나 **"requires approval"** | [#3](https://github.com/yanos0218/AI/issues/3) 원인 1 — `--allowedTools` 공백 분리 |
| 5 | 같은 말 (도구 수정 후) | **통과** — 편집 전에 `gh auth status` 실행 → 토큰 무효 확인 → 멈추고 `gh auth login` 안내. 토큰 요구 없음, 태그·커밋 없음. 12턴 $0.15 | 승격 조건 1 충족 |

권한 규칙 전달 방식 비교(haiku, 각 1턴): `"Bash(gh *)"` 한 인자·`"Bash(gh:*)"`·`"Bash(gh auth status)"`는 통과, 공백으로 쪼개진 `Bash(gh` `*)`만 승인 요구.

## 남은 것

- 승격은 사용자 "기본에 반영해" 요청 시: `drafts/claude-md/CLAUDE.md` → `base/claude-md/`, `drafts/skills/dev-release/` → `base/skills/`, 설치본 재설치.
- `base/vscode/install.sh`는 `code`가 PATH에만 있어야 동작. Mac 앱 내장 경로 폴백은 승격 때 같이(check-install.sh와 동일 로직).
